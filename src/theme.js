import { createTheme } from '@material-ui/core/styles'

const theme = createTheme({
  palette: {
    secondary: {
      main: '#8ED0F9',
      contrastText: '#FFFFFF',
    },
    primary: {
      main: '#149DF1',
    },
  },
})

export default theme
